/* Used by `next build` (Vercel). The Vite/vinext build configures PostCSS
   inline in vite.config.ts, so this file is ignored there. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
